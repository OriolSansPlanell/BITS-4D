# BiTS 4D runtime requirements
# Python 3.10+

# ============================================================================
# REQUIRED DEPENDENCIES
# ============================================================================

# GUI framework
PyQt5>=5.15.0

# Scientific computing
numpy>=1.22.0         # NumPy 1.x and 2.x both work
matplotlib>=3.5.0     # matplotlib.colormaps registry
scipy>=1.6.0          # Region growing, convex hulls, image filters
scikit-learn>=1.0.0   # K-means clustering (weighted), legacy classifier

# Image I/O
tifffile>=2021.0.0

# ============================================================================
# OPTIONAL DEPENDENCIES (feature-gated at runtime)
# ============================================================================

# Multi-level Otsu thresholding ("Run Otsu & Segment")
scikit-image>=0.18.0

# Excel statistics export
pandas>=1.3.0
openpyxl>=3.0.0

# GPU-accelerated histogram accumulation (CUDA). The application falls back
# to the CPU automatically when torch or a CUDA device is unavailable:
#   pip install torch --index-url https://download.pytorch.org/whl/cu118
# torch>=1.9.0
